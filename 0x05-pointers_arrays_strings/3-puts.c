#include"main.h"
/**
 * _puts(char *str) -  prints a string, followed by a new line, to stdout
 * @str: pointer of character type
 *
 * Return: void
 */
void _puts(char *str);
{
	printf("%s\n", str);
}
