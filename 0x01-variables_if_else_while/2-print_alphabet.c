#include<stdio.h>
/*
 * all code is inserted in the main
 */
int main(void)
{
	/*
	 * a program that prints lowercase alphabet
	 */
	char chr;

	for (chr = 'a'; chr <= 'z'; chr++)
	{
		putchar (chr);
	}
		putchar ('\n');
	return (0);
}
